/**
 * Hero.tsx — Re-exports Web3HeroAnimated as the main hero.
 * Pure CSS/React component (no Three.js, no GSAP).
 * Gradient dome background + stepped pillar silhouette + staggered copy.
 */
export { Web3HeroAnimated as default } from './ui/Web3HeroAnimated'
